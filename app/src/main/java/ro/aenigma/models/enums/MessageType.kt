package ro.aenigma.models.enums

enum class MessageType {
    TEXT,
    REPLY,
    DELETE,
    DELETE_ALL,
    GROUP_CREATE,
    GROUP_RENAMED,
    GROUP_MEMBER_ADD,
    GROUP_MEMBER_LEFT,
    GROUP_MEMBER_REMOVE,
}
